{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <h1>Edit Produit</h1>
    <a class="btn-info btn mb-2" href="{{ path('home') }}">back to list</a>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
